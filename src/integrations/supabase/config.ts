// Kept as a placeholder so any residual imports still resolve. The frontend-
// only build no longer talks to a Supabase project — these constants are
// meaningful only as a localStorage namespace.
export const SUPABASE_URL = "";
export const SUPABASE_PUBLISHABLE_KEY = "";
export const SUPABASE_PROJECT_ID = "smart-work-flow";